/**
 * Global Theme Configuration
 * Use this file to customize the look and feel of the ERP.
 */
export const THEME_CONFIG = {
    // Brand color (Primary)
    // Options: 'indigo', 'blue', 'violet', 'emerald', 'rose', 'amber', etc.
    // This will primarily affect the @theme block in globals.css if mapped.
    brandColor: 'indigo',

    // Global corner rounding
    // Options: 'none', 'sm', 'md', 'lg', 'xl', '2xl'
    // We will map this to a base radius variable.
    borderRadius: 'lg',

    // Clean UI settings
    compactMode: false,
    glassmorphism: true,
};
